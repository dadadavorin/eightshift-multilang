# Dependencies
/vendor/
/node_modules/

# Build output — webpack bundles are committed so the plugin works without a build step
# (remove /build/ from this file if you prefer to build on deploy)
/build/

# Composer
composer.lock

# Bun lockfile is committed (reproducible installs)
# npm / yarn lockfiles are not used
package-lock.json
yarn.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# WordPress
wp-config.php

# Environment
.env
.env.local

# Test artifacts
/tests/coverage/
.phpunit.result.cache

# PHPStan
.phpstan/
